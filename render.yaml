services:
  - type: web
    name: financial-planner-api
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: CLIENT_ORIGIN
        value: https://financial-planner-sd6r.onrender.com
